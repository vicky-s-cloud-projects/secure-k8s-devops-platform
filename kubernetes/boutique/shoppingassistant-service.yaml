apiVersion: v1
kind: Service
metadata:
  name: shoppingassistantservice
  namespace: boutique
spec:
  selector:
    app: shoppingassistantservice
  ports:
    - port: 8080
      targetPort: 8080